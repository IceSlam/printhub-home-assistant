# PrintHub Agent

PrintHub printing agent for Home Assistant.

The App connects to the PrintHub server over WSS and uses the already installed
CUPS Print Server through TCP/IPP. It does not access the printer USB device directly.

See `DOCS.md` for configuration and troubleshooting.
