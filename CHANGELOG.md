## 3.16.34

- HA App: основной XP-365B возвращён с PDF/PBM-фильтра на исходный CUPS Raster тракт из оригинального PPD (`application/vnd.cups-raster`).
- Добавлен архитектурно-независимый по исходникам native-фильтр `rastertosnailtspl-xprinter`, собираемый внутри HA App для amd64/aarch64/armv7. Он не масштабирует готовый 1-bit bitmap и использует точную геометрию CUPS Raster.
- Для профиля `58×40` снова используется исходная геометрия PPD: логический носитель 58×40 мм, печатный растр около 56×40 мм (~448×320 точек при 203 DPI).
- Исправлена печать нескольких копий: `cupsManualCopies=False`, поэтому CUPS больше не размножает страницу программно, а XP-365B получает один `PRINT N,1`. Несколько одинаковых этикеток выходят непрерывной лентой без паузы между копиями.
- AirPrint proxy переведён на ту же схему hardware copies и передаёт исходное число копий основной очереди.
- Защита от зависших заданий (`stop-printer` + cancel по тайм-ауту) сохранена.
- HA App обновлён до 2.2.24, встроенный Agent — до 1.5.5.

# Changelog

## 3.16.33
- Исправлена оставшаяся регрессия качества XP-365B: сравнение с последней заведомо исправной 3.16.30 и исходным `agent/XP-365B.ppd` показало, что в 3.16.31 одновременно с экспериментом 56 мм были изменены штатные параметры `PrintSpeed=12 → 5` и `Darkness=15 → 10`. В 3.16.32 масштабирование было отменено, но эти два изменения остались, поэтому видимые «зубцы» на границах штрихкода не исчезли.
- Восстановлен прежний профиль XP-365B: `PrintSpeed=12`, `Darkness=15` в HA App, CUPS PPD, PDF→TSPL фильтре и direct-TSPL пути Agent; удалён скрытый clamp скорости до 5.
- Для уже установленных HA App добавлена одноразовая совместимая миграция: если после 2.2.21/2.2.22 в Supervisor сохранилась ровно регрессионная пара `darkness=10` + `print_speed=5`, PrintHub автоматически применяет 15/12. Как только пользователь меняет любой из этих параметров, override снимается и дальнейшие значения полностью задаются пользователем.
- Исправление количества копий (`3 → 3`, без N×N), отмена зависшего CUPS job и `printer-error-policy=stop-printer` сохранены без отката.
- HA App обновлён до 2.2.23, встроенный Agent — до 1.5.4.

## 3.16.32
- Исправлена регрессия качества штрихкодов из 3.16.31: удалено принудительное сжатие 58-мм растра до 56 мм после преобразования PDF в монохромный PBM. Такое масштабирование 464→448 точек разрушало однопиксельную геометрию штрихкода и давало зубчатые вертикальные полосы.
- Для 58×40 снова используется нативный растр 464×320 точек при 203 DPI без промежуточного ресайза; `BITMAP` начинается с 0,0. Размер в UI/CUPS остаётся 58×40 мм.
- Аналогичное 56-мм сжатие удалено из direct-TSPL пути Agent для изображений. Защитные исправления количества копий, отмены зависшего CUPS job и `stop-printer` из 3.16.31 сохранены.
- HA App обновлён до 2.2.22, Agent — до 1.5.3.

## 3.16.31
- Исправлен критический баг количества копий: CUPS software copies больше не умножаются повторно TSPL-командой `PRINT N` (3→9 устранено; большие тиражи больше не превращаются в N×N).
- По тайм-ауту активное CUPS-задание теперь принудительно отменяется, чтобы после ошибки/замены рулона оно не продолжало печать в фоне.
- Для USB-очереди включён `printer-error-policy=stop-printer` вместо автоматического `retry-current-job`.
- Для `58×40` логический размер остаётся 58×40 во всех интерфейсах, а физический контент печатается в 56×40 с центрированием и ~1 мм боковыми полями; аналогичная коррекция применяется к другим 58-мм профилям.
- XP-365B ограничен штатной скоростью label mode 127 мм/с (5 ips); новые установки используют скорость 5 и плотность 10.
- HA App обновлён до 2.2.21, Agent — до 1.5.2.


## 3.16.30

- HA App обновлён до 2.2.20.
- Исправлена внутренняя навигация HA App: переходы по собственному sidebar больше не меняют `location.hash` и не создают навигацию ingress-iframe.
- Splash-screen снова относится только к загрузке/открытию HA App из интерфейса Home Assistant и не должен появляться при переключении разделов PrintHub.

## 3.16.29

- HA App обновлён до 2.2.19.
- Исправлена причина появления/залипания фирменного splash-screen при переходах по внутреннему sidebar HA App: splash больше не зависит от завершения CUPS/API-запросов.
- Splash-screen снова показывается при каждом новом открытии PrintHub из sidebar Home Assistant; ограничение «один раз за браузерную сессию» удалено.
- При недоступном или зависшем CUPS после splash отображается только обычный page loader/ошибка текущего раздела; Overview использует короткие 5-секундные CUPS probes вместо ожидания device discovery до 30 секунд.
- Внутренние page-request теперь отменяются при смене раздела, а бесполезный no-store warm-up CUPS API удалён, чтобы старые/фоновые запросы не конкурировали с текущей страницей.

## 3.16.28

- HA App обновлён до 2.2.18.
- Исправлено повторное появление первоначального фирменного splash-screen при переключении с других разделов Home Assistant обратно на PrintHub в desktop UI.
- Splash-screen теперь показывается только при первом открытии HA App в браузерной сессии; повторные ingress-загрузки используют обычный экран загрузки страницы.
- Убрана возможность краткого flash первоначального splash-screen до инициализации WebUI.

## 3.16.27

- Home Assistant Integration обновлена до 2.0.2.
- Исправлена ошибка `Migration handler not found` при обновлении существующей интеграции PrintHub с Config Entry версии 1 на версию 2.
- Добавлена штатная `async_migrate_entry`: существующие настройки Agent/Server сохраняются, недостающие поля схемы 2.0 заполняются безопасными значениями по умолчанию.
- Удалять и заново добавлять интеграцию после обновления не требуется.


## 3.16.26

- TG WebApp: в карточках локальных PDF количество страниц перенесено под название файла и выводится в одной строке справа от размера файла.
- TG WebApp: удалена надпись «Для повторной печати» из карточек локальной библиотеки.
- TG WebApp: из общего списка локальных PDF убраны степпер количества копий и кнопка «Печать» — печать и выбор количества выполняются только в модальном окне просмотра файла.
- TG WebApp: немного увеличен вертикальный отступ между строкой поиска/избранного и первой карточкой файла для выравнивания ритма интерфейса.

## 3.16.25

- TG WebApp: из локальной библиотеки удалён режим «Список» и переключатель вида; сохранён единый карточный режим отображения PDF.
- TG WebApp: размер этикетки/PDF (например, `58×40 мм`) перенесён из нижней части карточки непосредственно под мини-превью.
- TG WebApp: сетка карточки скорректирована под отдельную колонку превью с размером, а нижняя часть карточки оставлена только для количества копий и кнопки печати.

## 3.16.24

- Исправлена печать с iOS/AirPrint на актуальных сборках CUPS: операции `Send-Document` и `Close-Job` больше не требуют локального UNIX-пользователя `@OWNER`, что устраняет отказ для `requesting-user-name` с iPhone/iPad после security hardening CUPS 2026 года.
- Доступ к AirPrint остаётся ограничен сетевой политикой `<Location />`; административные и остальные операции управления заданиями по-прежнему требуют `@OWNER/@SYSTEM`.
- HA App обновлён до 2.2.17; при запуске автоматически мигрирует уже сохранённый `/data/cups/config/cupsd.conf`, поэтому ручной сброс CUPS-настроек не требуется.

## 3.16.23

- TG WebApp: в режиме «Список» действия «В избранное» и «Переименовать» окончательно закреплены в правом верхнем углу карточки локального PDF.
- TG WebApp: в режиме «Галерея» переработано мини-превью — убрана высокая вертикальная рамка, превью стало компактным горизонтальным блоком под формат этикетки.
- TG WebApp: строка управления копиями и кнопка «Печать» теперь занимает отдельную полную строку карточки; устранено налипание CTA на кнопку «+» на узких экранах.
- HA App обновлён до 2.2.16: бейдж «По умолчанию» у названия принтера выровнен по вертикальному центру относительно текста заголовка.

## 3.16.22

- TG WebApp: в режиме списка локальных PDF кнопки «В избранное» и «Переименовать» закреплены в правом верхнем углу карточки.
- TG WebApp: мини-превью локального PDF в режиме «Галерея» стало компактным и горизонтальным вместо высокой вертикальной рамки.
- TG WebApp: исправлена геометрия строки «Копии / Печать» — степпер теперь корректно сжимается, между кнопкой «+» и CTA «Печать» сохранён стабильный зазор на узких экранах.

## 3.16.21
- Telegram WebApp: исправлена компоновка карточки локального PDF — кнопка «Печать» и поле количества приведены к одинаковой геометрии, а «В избранное» и «Переименовать» перенесены в правый верхний угол карточки.
- Telegram WebApp: чекбокс «Сохранить в локальные» заменён на переиспользуемый switch в стиле раздела «Сервер» HA App.
- Telegram WebApp: кнопки обновления файлов получили фиксированную квадратную область и строго центрированную Font Awesome иконку.
- Telegram WebApp: действие «Загрузить PDF в локальные без печати» сокращено до «Загрузить PDF».

## 3.16.20
- Telegram WebApp: крестик в круглой кнопке закрытия модальных окон теперь центрируется строго по обеим осям; убраны влияние стандартного padding кнопки и baseline SVG.

## 3.16.19
- Telegram WebApp: карточки «Доступно для печати» переработаны в новый крупный layout по референсу: визуальное превью этикетки, marketplace/status badges, название товара, количество, ID отправления, отдельный размер и акцентная кнопка печати.
- Telegram WebApp: карточки локального хранилища приведены к тому же визуальному языку с увеличенным PDF-preview, badges, метаданными, размером этикетки, CopyStepper, печатью, избранным и переименованием.
- Компактный режим списка локальных PDF также получил обновлённую карточную поверхность и увеличенные previews.

## 3.16.18
- PrintHub HA App обновлён до 2.2.15: desktop burger скрыт окончательно, sidebar расширен до 300 px и mobile drawer теперь зависит только от ширины viewport.
- Telegram WebApp получил локальные Font Awesome Free Solid SVG-иконки для печати, обновления, загрузки, редактирования, удаления, закрытия, поиска, статуса и других уместных действий без CDN-зависимости.

## 3.16.17
- Home Assistant Integration обновлена до 2.0.1 и получила локальные фирменные brand assets PrintHub для карточки интеграции и экранов Home Assistant.

## 3.16.16
- PrintHub HA App обновлён до 2.2.14.
- Исправлена HTTPS bootstrap-сборка HA App: CA bundle копируется из полного `node:20-bookworm` stage до первого APT HTTPS-запроса.

## 3.16.15
- TG WebApp: экран прямого браузерного открытия теперь использует фирменный логотип PrintHub и дружелюбное описание платформы без сообщения о запрете доступа.
- TG WebApp: все стартовые notifications/toasts откладываются до полного исчезновения splash-screen; haptic и таймер уведомления также запускаются только после splash.
- HA App обновлён до 2.2.13: первичный startup loader заменён на тот же фирменный splash-screen, что используется в Telegram WebApp; внутренние page loaders не изменялись.

## 3.16.14
- PrintHub HA App обновлён до 2.2.12.
- Устранён дубликат AirPrint-принтера в iOS/macOS: удалена вторая Bonjour-публикация из Node, AirPrint публикуется только CUPS через `_ipp._tcp,_universal`.
- Dashboard HA WebUI теперь показывает фактическое имя и системную очередь CUPS, а не статический `XP365B_AirPrint`.
- AirPrint media profiles исправлены: 58×40/58×60/40×58 объявляются клиентам с правильными логическими размерами.

## 3.16.13
- Фирменный логотип PrintHub из Home Assistant App применён к основному Server / Telegram Mini App: favicon, WebApp header и assets для Telegram-бота.
- Добавлен Splash-screen Telegram WebApp: логотип PrintHub, подпись и loader показываются минимум ~1.5 секунды и остаются до завершения первичной авторизации/загрузки данных.
- Splash-screen плавно скрывается после boot; Telegram WebApp `ready()` вызывается сразу, поэтому вместо пустого экрана пользователь видит фирменную загрузку.
- В проект добавлены `server/assets/printhub-logo.svg` и `server/assets/printhub-logo.png`, пригодные для установки как аватар Telegram-бота через BotFather.

## 3.16.12
- PrintHub HA App обновлён до 2.2.11.
- Исправлена локальная сборка App на Home Assistant в сетях с заблокированным HTTP/80: Debian APT sources принудительно используют HTTPS/443, добавлены retries, IPv4 и сетевые timeouts.

## 3.16.11
- PrintHub HA App обновлён до 2.2.10.
- Исправлено обнаружение AirPrint через явную Bonjour/mDNS-публикацию `_ipp._tcp,_universal` и обновление CUPS DNS-SD subtype.
- Desktop WebUI: burger скрыт, постоянный левый sidebar расширен.

## 3.16.10
- PrintHub HA App обновлён до 2.2.9.
- В HA WebUI добавлены стартовый loader, фирменная иконка PrintHub, упрощённые страницы Agent/Справка, мобильные заголовки страниц и переработанные switch-style формы.
- AppSelect полностью переработан в автономный JS-компонент без Vue/SFC и без fallback `<select>`.
- Удалена зависимость сборки HA WebUI от Vite/Vue.

## 3.16.8
- PrintHub HA App обновлён до 2.2.7.
- Исправлена надёжная инициализация Vue SFC AppSelect в HA Ingress.
- Сохранение CUPS server settings переведено с `cupsctl` на атомарное управление `cupsd.conf` с валидацией и rollback.

## 3.16.7
- PrintHub HA App обновлён до 2.2.6.
- AppSelect переработан в настоящий Vue SFC dropdown/listbox без нативного select в штатном режиме.
- Исправлена загрузка AppSelect и автоматический upgrade fallback-контролов.
- Исправлена ошибка `cupsctl: Unable to connect to server: Bad file descriptor` — CUPS admin client теперь использует `127.0.0.1:631`.

## 3.16.6
- Bundled PrintHub HA App обновлён до 2.2.5.
- Исправлена критическая ошибка полностью пустого WebUI после обновления: основной JS больше не блокируется загрузкой SFC AppSelect bundle.
- Исправлена кнопка открытия sidebar и статические иконки toolbar.

## 3.16.5
- PrintHub HA App обновлён до 2.2.4.
- Все псевдоиконки WebUI заменены на единый локально встроенный набор Font Awesome Free Solid.
- Исправлен SFC AppSelect в разделе заданий и выборе очереди для переноса задания.
- Исправлена ошибка `cupsctl: Unable to connect to server: Bad file descriptor` переводом административных CUPS-команд на локальный Unix socket.

## 3.16.4
- PrintHub HA App обновлён до 2.2.3.
- Нижняя мобильная навигация удалена полностью; все разделы находятся только в левом sidebar.
- На mobile/tablet sidebar работает только как off-canvas drawer по кнопке `☰`.
- Удалены конфликтующие responsive-правила нижней/горизонтальной навигации.
- Сохранены SFC AppSelect, SVG-иконки и полные переводы настроек Home Assistant.

## 3.16.3
- PrintHub HA App обновлён до 2.2.2.
- Исправлена фактическая причина отображения старого мобильного WebUI после обновления: CSS/JS больше не кешируются на 3600 секунд, все WebUI assets отдаются с `no-store` и имеют version cache-buster.
- Мобильная навигация окончательно переведена в левый sidebar/drawer по кнопке меню; нижний navbar отсутствует.
- Усилены обработчики открытия/закрытия sidebar и выбора разделов.
- Сохранены Vue SFC `AppSelect`, SVG-иконки кнопок и полные переводы Home Assistant App schema.

## 3.16.2
- Исправлен PrintHub HA App без отката функций 2.2.0: сохранены Vue SFC `AppSelect`, SVG-иконки кнопок и полные `ru/en` translations.
- Мобильный нижний navbar полностью удалён и заменён выезжающим левым sidebar по кнопке меню.
- Убраны дубли верхнеуровневых заголовков страниц; название страницы остаётся только в верхнем topbar.
- Исправлено открытие/закрытие sidebar, backdrop, Escape и закрытие после выбора раздела.

## 3.16.0
- PrintHub HA App обновлён до 2.2.0.
- Все select в modern HA WebUI переведены на общий Vue SFC `AppSelect`.
- Ко всем кнопкам modern WebUI добавлены семантические SVG-иконки.
- Полностью локализованы заголовки и описания всех Home Assistant App settings на русском и английском языках.

## 3.15.4
- Дополнительно исправлен mobile lower navigation bar в PrintHub HA App: увеличен breakpoint мобильного режима, увеличены иконки/подписи и уменьшено пустое пространство под панелью.

## 3.15.3
- Обновлён современный WebUI PrintHub HA App для мобильных экранов: нижняя навигация стала крупнее, адаптивнее и теперь примыкает к нижней кромке экрана без лишнего пустого отступа.
- Улучшена реактивность/адаптивность Ingress WebUI: мобильное меню стало горизонтально прокручиваемым, а страницы автоматически подстраиваются под узкие экраны.
- Добавлено автообновление состояний в WebUI для обзорных страниц и диагностики.
- Статусы заданий и большинство параметров печати локализованы на русский язык.
- Настройки принтера и сервера CUPS переведены на более понятные русские формулировки.

## 3.15.2
- Bundled PrintHub All-in-One Home Assistant App обновлён до 2.1.1.
- Исправлен `webui` placeholder для актуального Supervisor: `[PORT:8099]`.
- Локальная установка App больше не должна отклоняться при `ha store reload`.

## 3.15.0
- PrintHub All-in-One HA App upgraded to 2.1.0.
- Home Assistant integration upgraded to 2.0.0 and specialized for the combined Agent + CUPS + AirPrint App.
- Added local `/overview` contract exposing CUPS server, queues, AirPrint, USB, jobs, classes and App configuration.
- Added loopback-only printer controls for HA test printing and queue purge.
- Added dedicated All-in-One HA entities while preserving all existing entity unique IDs.
- Added reconfigure flow for the custom integration.
- Legacy `/status` remains available as an upgrade-order fallback.

## 3.14.0
- Home Assistant deployment changed from two Apps (Agent + CUPS) to one all-in-one `PrintHub` App 2.0.0.
- Added modern Ingress CUPS administration WebUI with printers, jobs, classes, devices, PPD/driver management, server settings, logs and Agent diagnostics.
- Classic CUPS WebUI remains available on port 631 as compatibility fallback.
- CUPS configuration, printers and classes persist under `/data/cups/config`.

## 3.13.0
- Добавлен собственный Home Assistant App `PrintHub CUPS` 1.0.0 для полной замены стороннего CUPS App.
- App содержит CUPS, USB auto-discovery XP-365B, встроенный PDF→TSPL драйвер без проприетарного Xprinter `.deb`, PPD и AirPrint.
- Основная очередь `XP365B` остаётся совместима с PrintHub Agent и профилями 58×40 / 40×58 / 58×60 / 75×120 / 120×75.
- Добавлена отдельная AirPrint proxy-очередь с фиксированным размером 58×40 по умолчанию.
- AirPrint queue пересылает PDF во внутреннюю `XP365B`, поэтому только одна очередь напрямую владеет USB-принтером.
- DNS-SD/Bonjour публикация выполнена через CUPS + host D-Bus Home Assistant, без второго Avahi daemon.
- Добавлена автоматическая пересборка очередей после USB/App restart.
- Добавлен CUPS Web UI с настраиваемыми учётными данными.

## 3.12.4
- Исправлена неработающая кнопка подтверждения удаления в WebApp PDF-preview.
- Причина: `confirmDeletePreview()` обращался к `deletingId` как к локальной переменной, хотя это prop; теперь используется `props.deletingId`.
- Удаление локального PDF вынесено в общий серверный helper `deleteLocalLibraryFile()`.
- В карточку локального PDF Telegram-бота добавлена кнопка `🗑 Удалить локальный файл`.
- В Telegram-боте добавлено двухэтапное подтверждение удаления `Отмена / 🗑 Удалить`.
- WebApp и бот теперь используют один и тот же серверный путь удаления: оригинальный PDF + preview-cache + запись `library.json`.
- История уже выполненной печати при удалении локального PDF сохраняется.
- PrintHub Agent и Home Assistant integration в этом patch не изменялись.

## 3.12.3
- PrintHub Home Assistant Integration обновлена до 1.0.1.
- Timestamp `Последнее сообщение сервера` теперь выводится в локальном часовом поясе HA в формате `ЧЧ:ММ ДД.ММ.ГГГГг.`.
- Форматирование выполняется только в entity; Agent API сохраняет исходные ISO timestamps.

## 3.12.2
- Исправлен header PDF-preview на iPhone: `Избранное` и `Закрыть` теперь всегда находятся в одной горизонтальной строке.
- Кнопка `Избранное` расположена непосредственно слева от `×`.
- Кнопка-звезда в PDF-preview теперь полностью без рамки, фона и обводки; активное состояние обозначается только золотым цветом звезды.
- Исправлена причина вертикального расположения: базовое правило `.pdf-preview-header > div` больше не может превратить контейнер header-actions в grid.
- Исправлено подтверждение удаления для длинных имён PDF: filename теперь корректно переносится и не расширяет min-content модалки.
- Сетка `Отмена / Удалить` переведена на `minmax(0,1fr)` и обе кнопки принудительно ограничены шириной контейнера.
- Кнопка `Удалить` больше не выходит за правую границу preview и сохраняет полноценную touch-area на iPhone.
- Добавлены safe-area отступы footer по горизонтали.

## 3.12.1
- Bundled PrintHub Home Assistant Agent App обновлён до 1.1.2.
- Исправлен crash локального `/status` API из-за рассинхронизации `homeassistant-entrypoint.js` и `agent.js`.
- Agent core обновлён до 1.5.1 с экспортируемыми status snapshot API и WebSocket telemetry.

## 3.12.0
- Иконка избранного в карточном виде локальной библиотеки перенесена в правый верхний угол PDF-превью.
- Звезда реализована отдельной overlay-кнопкой-соседом, а не вложенной кнопкой, поэтому нажатие на остальную область превью по-прежнему открывает PDF.
- Нажатие на звезду использует отдельный обработчик и не открывает карточку.
- Кнопка печати в галерее и списке перенесена под `− / N / +`, что устраняет горизонтальный вылет на узких экранах.
- Кнопка переименования в режиме списка приведена к той же иконке `✎`, что и в карточном виде.
- Добавлено удаление локального PDF через развернутую карточку/preview с отдельным подтверждением.
- Кнопки удаления в общей галерее и списке намеренно отсутствуют.
- Сервер получил `DELETE /api/webapp/local-files/:id`, удаляющий PDF, его preview-cache и запись из `library.json`.
- После удаления preview закрывается штатной leave-анимацией, а связанные thumbnail Object URL очищаются.
- Модалка `Агент и принтер` переведена на тот же transition `sheet`, что и ручная печать: общий fade overlay, полный slide-to-bottom, одинаковые easing/duration, stagger появления header/body и scroll-lock до `after-leave`.
- Логика печати, PrintHub Agent и Home Assistant integration не изменялась.

## 3.11.1
- Удалён accordion `История печати`.
- История теперь постоянно отображается как обычный блок внизу страницы.
- Заголовок содержит количество последних заданий и отдельную кнопку обновления.
- Внутренний список ограничен примерно четырьмя обычными карточками; остальные записи доступны через вертикальный scroll.
- Карточки с длинными ошибками сохраняют естественную высоту и не обрезаются.
- Анимация появления/изменения записей истории сохранена.
- Модалка `Агент и принтер`, PrintHub Agent, HA integration и логика печати не изменялись.

## 3.11.0
- Индикатор `Агент` в шапке переименован в `Онлайн / Офлайн`.
- Статус в шапке получил собственную цветную точку без текстового `Агент`.
- Рядом со статусом добавлена круглая кнопка `!` в обводке.
- По кнопке `!` открывается отдельная модалка `Агент и принтер`.
- В модалку перенесён весь диагностический функционал прежнего нижнего аккордеона: последняя связь, режим подключения, CUPS scheduler/socket/очереди, диагностика, IP, последняя печать, busy/active job, очередь, PDF-режим и версия агента.
- Кнопка `Обновить статус` перенесена в модалку.
- Нижний аккордеон `Агент и принтер` полностью удалён из основного потока страницы.
- Модалка получила плавное открытие/закрытие, scroll lock, mobile bottom-sheet режим и desktop centered режим.
- История печати остаётся отдельным нижним аккордеоном.
- PrintHub Agent, Home Assistant integration и серверная логика печати не изменялись.

## 3.10.0
- Добавлен мини-поиск по локальной библиотеке PDF без дополнительных запросов к серверу на каждый символ.
- Поиск работает по отображаемому имени, исходному имени PDF и размеру/профилю этикетки.
- Добавлен фильтр «Избранное» со счётчиком и быстрым сбросом фильтров.
- Для карточек, списка и PDF-preview добавлена кнопка ☆/★.
- Состояние избранного сохраняется в `library.json` и переживает перезапуск PrintHub.
- Избранные PDF автоматически сортируются выше остальных.
- Telegram-бот получил отметку избранного, отдельный просмотр избранных PDF и поиск по локальной библиотеке.
- Старые записи библиотеки автоматически мигрируют с `favorite: false`.

## 3.9.1

- WebApp: кнопка «Авто» в ручной печати теперь занимает всю ширину, а ниже вынесены три явные кнопки размеров: 58×40, 58×60 и 75×120.
- WebApp/Bot/Server/Agent: добавлен новый профиль печати 58×60 мм.
- PPD: в основной PPD добавлен новый размер 58×60 мм, а также добавлен отдельный AirPrint PPD для 58×60.


## 3.9.0
- Telegram-бот получил полноценное главное меню, повторяющее основные возможности WebApp.
- Добавлены разделы бота: быстрая печать PDF, загрузка PDF в локальное хранилище без печати, локальная библиотека, файлы маркетплейсов, история, статус агента/принтера и запуск WebApp.
- Добавлена state-machine для сценариев загрузки: `quick_print`, `save_local`, `rename_local`.
- В боте локальные PDF можно просматривать, переименовывать и печатать с выбором количества копий.
- В боте для количества копий добавлены inline-кнопки `− / N / +`.
- Для файлов маркетплейсов в боте добавлен выбор количества копий до печати.
- Для ручной PDF-печати через меню бота добавлен выбор размера и количества копий перед отправкой.
- Сохранение PDF в локальную библиотеку через бот больше не создаёт print job.
- В WebApp добавлена отдельная кнопка «Загрузить PDF в локальные без печати».
- Добавлен отдельный WebApp endpoint `POST /api/webapp/local-files/upload`.
- В ручной печати, карточках локальной библиотеки, списке и PDF-preview числовые поля заменены на полноценные `− / N / +` stepper controls.
- Прямой PDF, отправленный боту без выбора действия, по-прежнему печатается одной копией для обратной совместимости.
- PrintHub Agent и Home Assistant integration не изменялись.

## 3.8.1
- Исправлен `SmoothCollapse.vue`: содержимое аккордеона больше не исчезает после завершения enter-анимации.
- Базовый CSS-state `.smooth-collapse` теперь является открытым и видимым (`height:auto`, `opacity:1`, `transform:none`).
- Скрытое состояние задаётся только временными inline-стилями во время открытия/закрытия.
- Transition переведён в JS-controlled режим `:css="false"` с явными `done()` и fallback-таймером для Telegram WebView.
- Добавлена корректная обработка `enter-cancelled` / `leave-cancelled` при быстром повторном нажатии.
- Плавное измеряемое закрытие истории, агента и локальной библиотеки сохранено.
- Другие части PrintHub, Agent и Home Assistant integration не изменялись.

## 3.8.0
- История печати переведена с CSS Grid на flex-список карточек естественной высоты.
- Блок ошибки теперь является отдельной полноширинной частью карточки и физически увеличивает её высоту.
- Удалено поведение, при котором соседняя карточка могла визуально наезжать на длинную ошибку при TransitionGroup.
- Добавлены natural-height rows, scroll padding и отключено scroll anchoring для фоновых обновлений истории.
- Все accordion-блоки переведены на общий `SmoothCollapse.vue` с измерением реальной высоты через `scrollHeight`.
- История, агент и локальная библиотека теперь плавно закрываются по фактической высоте содержимого.
- Убран `position: fixed` у body и `window.scrollTo()` при закрытии модалок — источник заметного скачка в Telegram/iOS.
- Ручная модалка теперь непрерывно уезжает полностью вниз при закрытии.
- PDF preview получил более длинный непрерывный fade/translate exit.
- Увеличена плавность карточек, фильтров, кнопок, loading-состояний и scroll-контейнеров.
- Логика печати, PrintHub Agent и Home Assistant integration не изменялась.

## 3.7.1
- Исправлены карточки ошибок в истории печати.
- Удалена фиксированная высота 96px, из-за которой failed-job визуально обрезался.
- Ошибка больше не ограничивается двумя строками и отображается полностью.
- Длинные команды `lp` и сообщения CUPS корректно переносятся внутри карточки.
- Failed-job получил отдельный визуальный блок ошибки.
- На мобильных статус и время выводятся под содержимым карточки.
- Сам список истории остаётся ограниченным по высоте и прокручиваемым.

## 3.7.0
- Полностью переработана система UI-анимаций Mini App.
- Исправлено дёргание закрытия ручной модалки и PDF preview.
- Состояние и содержимое модалки сохраняются до `after-leave`, body scroll снимается только после окончания exit-анимации.
- Добавлен общий lock manager для вложенных модальных окон.
- Добавлен глобальный стек уведомлений поверх всех модалок.
- Уведомления показываются при отправке задания, успешной постановке в очередь, ошибке, завершении/ошибке печати и смене online-состояния агента.
- Добавлены Telegram haptics для success/error/warning, когда API доступен.
- Добавлены анимации документов, фильтров, карточек, accordion content, loading/sending state и status badges.
- PrintHub Agent обновлён до 1.5.0.
- PrintHub Agent Home Assistant App обновлён до 1.1.0 и получил локальный status API `127.0.0.1:35994`.
- Добавлена полноценная Home Assistant custom integration `printhub` 1.0.0 с Config Flow.
- Интеграция создаёт sensor/binary_sensor/button entities на основе Agent/CUPS и опционально PrintHub Server.
- Добавлены diagnostics с редактированием ADMIN_API_KEY.

## 3.6.2
- Исправлена разметка `LocalFiles.vue` после добавления анимированного accordion.
- Удалён лишний закрывающий `</div>`, вызывавший ошибку Vite/Vue `Invalid end tag`.
- Все UI-анимации версии 3.6.1 сохранены.
- Логика PrintHub Server и Agent не изменена.

## 3.6.1
- Добавлена единая motion-система интерфейса.
- Плавный переход между авторизацией и основным экраном.
- Последовательное появление основных разделов.
- Анимированное открытие/закрытие истории, агента и локальной библиотеки.
- Slide/scale для модального окна ручной печати.
- Fade/scale для PDF preview.
- Slide/fade/scale для уведомлений.
- Анимация появления новых записей истории.
- Лёгкая анимация нажатия кнопок.
- Поддержка prefers-reduced-motion.
- API и протокол агента не изменены.

## 3.6.0
- Добавлен отдельный Home Assistant App `PrintHub Agent`.
- Shared Agent обновлён до 1.4.0.
- Добавлен `PRINTER_CONNECTION_MODE=cups`.
- Home Assistant Agent работает через установленный CUPS App по TCP/IPP.
- Home Assistant Agent не требует raw USB и не запускает второй CUPS daemon.
- Добавлены Home Assistant `config.yaml`, Dockerfile, translations, options schema и документация.
- Standalone Docker Agent сохранён без замены.

## 3.5.4
- Agent обновлён до 1.3.2.
- Добавлена последовательная очередь печатных заданий (concurrency = 1).
- WebSocket heartbeat отделён от выполнения CUPS job.
- Добавлен native WebSocket ping/pong и контролируемый reconnect.
- Результаты завершённого задания буферизуются при кратковременном разрыве связи.
- Убрана параллельная CUPS health-проверка во время активной печати.
- В Mini App отображаются busy/active job/queue агента.
- Кнопки `Авто / 58×40 / 75×120` сделаны компактнее и размещены в один ряд.
- Область выбора PDF уменьшена по высоте.
- История по-прежнему содержит 50 записей, но блок показывает около 4 карточек и имеет внутренний скролл.
- Длинные ошибки истории ограничены двумя строками.

## 3.5.3
- Agent обновлён до 1.3.1.
- `SERVER_URL` больше не является обязательной переменной запуска.
- `WS_URL` больше не является обязательной переменной запуска.
- Агент умеет автоматически вычислять HTTP URL из WebSocket URL и наоборот.
- Для текущего PrintHub добавлены штатные defaults `print.iceslam.ru`.
- `docker-compose.agent.yml` явно передаёт SERVER_URL/WS_URL/AGENT_ID/CUPS_SERVER.
- Обязательным секретом остаётся только `AGENT_TOKEN`.
- Ошибка отсутствующего `AGENT_TOKEN` теперь содержит понятную инструкцию.
- В startup log добавлена итоговая конфигурация агента без токена.

## 3.5.2
- Исправлен stale bind mount `/run/cups/cups.sock`: контейнер теперь монтирует весь `/run/cups`.
- Agent 1.3.0 умеет ждать восстановления CUPS scheduler и повторять transient failures.
- Добавлен CUPS health monitor и отображение состояния scheduler/socket/очередей в Mini App.
- Добавлено ожидание завершения CUPS job после `lp accepted`.
- Добавлен Docker healthcheck для host CUPS socket.
- Добавлен `host/setup-orange-pi.sh`: persistent journald, CUPS restart policy, self-healing timer, USB autosuspend rule, watchdog.

## 3.5.1
- Блок ручной печати на главном экране заменён на кнопку открытия отдельного раздела.
- Форма ручной печати перенесена в модальное окно / sheet.
- Блок `Локально загруженные` перемещён внутрь раздела ручной печати.
- Кнопки выбора размера переработаны в визуальные карточки с подписями.
- На кнопке открытия раздела показывается количество файлов в локальной библиотеке.

## 3.5.0
- Добавлена фото-галерея локально сохранённых PDF.
- Первая страница нового PDF автоматически рендерится в WebP thumbnail.
- Для существующей библиотеки реализована ленивая генерация превью.
- Добавлено кэширование preview в `data/previews`.
- Добавлен переключатель `Галерея / Список`, галерея используется по умолчанию.
- Нажатие на thumbnail открывает полноэкранный просмотр.
- Для многостраничных PDF добавлено перелистывание страниц.
- Из окна предпросмотра можно выбрать количество копий и отправить исходный PDF на печать.
- Preview API защищён Telegram WebApp авторизацией.
- Серверный Docker image теперь устанавливает `poppler-utils`.

## 3.4.8
- Agent обновлён до 1.2.2.
- В `auto` агент проверяет существование CUPS-очередей через `lpstat` до запуска `lp`.
- Несуществующая `XP365B_IP` теперь пропускается и не маскирует результат USB-печати.
- Для `usb`/`ip` режима отсутствующая очередь возвращает понятную диагностическую ошибку.
- При нескольких ошибках `auto` возвращается объединённая диагностика всех кандидатов.
- В лог добавлены `CUPS queue available`, `CUPS queue unavailable; skipping` и `CUPS print accepted`.

## 3.4.7
- Отключён автоматический zoom Telegram Mini App при фокусе на полях ввода в iOS.
- Viewport зафиксирован с `maximum-scale=1` и `user-scalable=no`.
- `input`, `select` и `textarea` используют минимум 16px для предотвращения iOS focus zoom.
- Исправление распространяется на количество копий и переименование локальных файлов.

## 3.4.6
- Раздел `Локально загруженные` перенесён после marketplace-документов.
- Локальная библиотека PDF оформлена как аккордеон.
- В заголовке аккордеона отображается количество сохранённых файлов.
- Переименование, количество копий и повторная печать сохранены внутри раскрываемого блока.

## 3.4.5
- Удалены `CustomPageSize`, `ParamCustomPageSize`, `MaxMediaWidth` и `MaxMediaHeight`.
- CUPS больше не должен рекламировать произвольный диапазон custom media.
- Добавлены single-size PPD для AirPrint: 58×40, 75×120, 40×58, 120×75.
- Универсальный PPD PrintHub сохранён с четырьмя marketplace-размерами, но без custom media.

## 3.4.4
- PPD очищен от всех лишних и дюймовых размеров бумаги.
- В CUPS оставлены только `58×40`, `40×58`, `75×120`, `120×75`.
- Сохранена физическая ширина 56 мм для профиля, отображаемого как `58×40`.
- Сохранена физическая высота 56 мм для профиля, отображаемого как `40×58`.

## 3.4.3
- Исправлена ошибка Ozon `PostingFbsUnfulfilledListRequest.Limit: value must be inside range (0, 100]`.
- `limit` для `/v4/posting/fbs/unfulfilled/list` изменён с 1000 на 100.
- Добавлена полноценная пагинация через `offset` по 100 отправлений.
- Добавлена дедупликация по `posting_number`.
- Добавлена защита от повторяющейся страницы API.
- Добавлен `OZON_FBS_MAX_PAGES`, по умолчанию 50 страниц / 5000 отправлений.

## 3.4.2
- Исправлен запрос списка необработанных FBS-отправлений Ozon.
- Добавлены обязательные `sort_dir`, `filter.cutoff_from`, `filter.cutoff_to`.
- Ozon теперь запрашивается сразу со статусом `awaiting_deliver`.
- Добавлены `OZON_FBS_CUTOFF_PAST_DAYS` и `OZON_FBS_CUTOFF_FUTURE_DAYS`.
- Удалён слепой fallback v4 → v3, который скрывал исходную ошибку API.
- Ошибки marketplace-интеграций в WebApp теперь показывают нормальные названия площадок.

## 3.4.1
- Исправлено переполнение WebApp длинными именами PDF в блоке ручной загрузки.
- Имя выбранного файла ограничено двумя строками и остаётся внутри drop-zone.
- Добавлена защита от горизонтального overflow всего приложения.
- Дополнительно ограничены длинные названия marketplace-файлов, локальной библиотеки и истории.
- В PPD добавлены отсутствовавшие варианты PrintSpeed 6–12, поэтому DefaultPrintSpeed=12 теперь является валидным выбором CUPS.

## 3.4.0
- Добавлено количество копий при ручной печати (1–99).
- Добавлен чекбокс `Сохранить в локальные`, по умолчанию выключен.
- Добавлена постоянная локальная библиотека PDF в `data/library`.
- Добавлено переименование сохранённых PDF.
- Добавлена повторная печать локального PDF с выбором количества копий.
- Добавлен нижний аккордеон `История печати`.
- История реактивно обновляет состояния pending/sent/printing/done/failed.
- Аккордеон агента остаётся в самом низу страницы.
- Исправлена дублирующая строка разбора WebSocket URL из 3.3.1.

## 3.3.1
- Исправлен WebSocket handshake агента.
- `AGENT_TOKEN` теперь проверяется до HTTP 101.
- Добавлен Bearer token как резерв к query token.
- Добавлен RFC6455 ping/pong heartbeat.
- Добавлены close code/reason и unexpected-response логи агента.
- `docker-compose.agent.yml` больше не содержит демонстрационный hardcoded token; используется `.env.agent`.
- Agent version 1.2.1.

## 3.3.0
- Telegram Web App полностью переписан на Vue 3 + Vite и Vue SFC.
- Данные документов и статуса агента обновляются реактивно без перезагрузки интерфейса.
- Проверка авторизации остаётся полным auth-gate: основной UI не создаётся до успешной проверки Telegram `initData` сервером.
- Настройки/статус агента перенесены в аккордеон в самый низ страницы.
- Сервер самостоятельно мониторит новые доступные документы Ozon/WB/Яндекс и уведомляет обоих разрешённых пользователей.
- В Telegram-уведомлении есть inline-кнопка `Печать`, которая создаёт задание без открытия Mini App.
- Состояние доступных marketplace-файлов сохраняется в `data/marketplace-state.json`, чтобы после рестарта не дублировать уведомления.
- Добавлены `MARKETPLACE_NOTIFY_MS` и `MARKETPLACE_NOTIFY_INITIAL`.
- В серверный лог добавлен код/reason отключения агента.

## 3.2.0
- Telegram Web App теперь полностью закрыт auth-gate: интерфейс не показывается до успешной серверной проверки Telegram `initData`.
- Доступ к Web App разрешается только ID из `TELEGRAM_ALLOWED_USER_IDS`; прямое открытие URL показывает только экран отказа/авторизации.
- Добавлен блок статуса офисного агента: online/offline, last seen, режим подключения, USB/IP CUPS-очереди и последний использованный транспорт.
- Сервер хранит heartbeat/metadata подключённого агента.
- Агент 1.2.0 поддерживает `PRINTER_CONNECTION_MODE=auto|usb|ip`.
- USB/CUPS является основным способом печати; IP/CUPS может использоваться как резерв без потери качества.
- `PRINTER_HOST` больше не обязателен для USB-only конфигурации.
- В режиме `auto` агент сначала печатает через `CUPS_PRINTER_USB`, а при ошибке пробует `CUPS_PRINTER_IP`.

## 3.1.0
- Telegram Web App для двух администраторов.
- Проверка Telegram initData и whitelist Telegram ID.
- Ручная загрузка PDF с Auto / 58×40 / 75×120.
- Marketplace printables: Ozon, WB, Яндекс Маркет без вывода заказов.
- Автоматическое исчезновение документов после изменения статуса через live sync.
- Сохранён сценарий отправки PDF в личные сообщения Telegram-боту.
- Агент обновлён до CUPS/PDF auto-profile версии.

## 3.0.0
- Telegram-first auto print.
